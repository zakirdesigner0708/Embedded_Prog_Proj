#include<stdio.h>
int main() {
    int array[] = {7, 12, 9, 4, 11};  // Corrected array declaration
    
    for (size_t i = 0; i < count; i++)
    {
        /* code */
    }
    

    printf("%d\n", array[0]);         // Corrected printf statement
    return 0;
}